<!DOCTYPE html>
<html lang="en">
<head>
    <title>SOAR</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <script src="https://kit.fontawesome.com/2ea5bc6e34.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans|Open+Sans+Condensed:300,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="stylesheets/style.css">
</head>
<div class="container-fluid col-12 p-0 d-flex align-items-center h-100">
    <div class="row p-0 col-12 justify-content-center">
        <div class="col-lg-3 col-md-5 col-11 p-0">
            <div class="row col-12 p-0">
                <div class="col col-12 border border-dark m-1">
                    <p class="m-0 p-4">SOAR</p>
                </div>
                <div class="col-12 border border-dark m-1">
                    <form action="/register" method="post">
                    <p class="left"> Email</p>
                    <input name="email" type="email" class="form-control" aria-describedby="emailHelp" placeholder="Enter email">
                </div>
                <div class="col-12 border border-dark m-1">
                    <p class="left"> Password</p>
                    <input name="password" type="password" class="form-control" placeholder="Password">
                </div>
                <div class="col-12 border border-dark m-1">
                    <p class="left"> Confirm Password</p>
                    <input name="passwordConfirm" type="password" class="form-control" placeholder="Password">
                </div>
                <div class="col col-12 border border-dark m-1">
                    <button type="submit" class="btn btn-light btn-lg btn-outline-dark centre">Create Account</button>
                    </form>
                </div>
                <% if (locals.errors && locals.errors.register) { %>
                    <div class="row col-12 p-0 m-1">
                        <% locals.errors.register.forEach(function(error) { %>
                            <div class="col-12 div alert alert-primary alert-dismissible fade show border-dark border" id="textcentre" role="alert">
                                <p><%= error %></p>
                                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                    <span aria-hidden="true">&times;</span>
                                </button>
                            </div>
                        <% }) %>
                    </div>
                <% } %>
                <div class="col col-12 border border-dark m-1">
                    <button type="button" class="btn btn-light btn-lg btn-outline-dark centre"><a href="/">login</a></button>
                </div>
            </div>
        </div>
    </div>
</div>
<%- include('partials/footer'); %>